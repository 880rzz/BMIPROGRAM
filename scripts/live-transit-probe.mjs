const endpoint='https://vao.demo.hafas.de/gate';
const d=new Date(Date.now()+7*86400000);const y=d.getUTCFullYear(),m=String(d.getUTCMonth()+1).padStart(2,'0'),day=String(d.getUTCDate()).padStart(2,'0');
const loc=(lat,lon)=>({type:'C',crd:{x:Math.round(lon*1e6),y:Math.round(lat*1e6)}});
const body={svcReqL:[{req:{depLocL:[loc(48.2082,16.3738)],arrLocL:[loc(48.2114,16.3770)],jnyFltrL:[{type:'PROD',mode:'INC',value:'1023'}],getPolyline:false,getPasslist:false,outDate:`${y}${m}${day}`,outTime:'163000',outFrwd:false,numF:3},meth:'TripSearch',id:'1|1|'}],client:{id:'VAO',v:'1',type:'AND',name:'nextgen'},ver:'1.73',lang:'de',auth:{aid:'nextgen',type:'AID'}};
const ctl=new AbortController();const t=setTimeout(()=>ctl.abort(),12000);
try{const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json','Origin':'https://programvalaszto.magyariskola.at'},body:JSON.stringify(body),signal:ctl.signal});if(!r.ok)throw new Error(`HTTP ${r.status}`);const cors=r.headers.get('access-control-allow-origin')||'';const j=await r.json();const cons=j?.svcResL?.[0]?.res?.outConL||[];if(!cons.length)throw new Error('No transit connections returned');if(cors!=='*'&&!cors.includes('programvalaszto.magyariskola.at'))throw new Error(`Browser CORS not allowed: ${cors||'missing header'}`);console.log(`PASS: VAO transit returned ${cons.length} connections and browser CORS is ${cors}.`)}finally{clearTimeout(t)}
